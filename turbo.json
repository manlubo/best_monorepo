{
  "$schema": "https://turbo.build/schema.json",

  "globalDependencies": [
    "packages/config/**",
    ".editorconfig",
    "pnpm-lock.yaml"
  ],

  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },

    "typecheck": {
      "outputs": []
    },

    "build": {
      "dependsOn": ["^build", "typecheck"],
      "outputs": ["dist/**", ".next/**"]
    },

    "lint": {
      "outputs": []
    },

    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    }
  }
}
